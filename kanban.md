# AETHER Development Kanban Board

## READY (To-Do)
- [ ] **Implement real embeddings in `memory/vector_store.py`** (currently uses dummy vectors).
- [ ] **Fix entity resolution logic in `memory/entity_resolver.py`** (replace `eval()` with `json.loads()` and use vector store for candidate narrowing).
- [ ] **Improve task parsing in `orchestration/engine.py`** (better mapping from text to tool calls).

## IN PROGRESS
- None

## DONE
- Created Kanban board tracking system.
